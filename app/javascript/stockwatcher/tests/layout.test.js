import StockView from '../views/layout'

describe('My View', () => {
  it('should expect true', () => {
    expect(true); //just testing that jest is working correctly at the moment, will replace soon with some actual tests
  })
})
